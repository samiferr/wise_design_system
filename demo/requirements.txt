Django>=5.0,<6.0
django-filter>=24.0
djangorestframework>=3.15

# Optional, used only by the Media -> QR Code & Barcode docs page. wise_core's
# {% qr_code %}/{% barcode %} tags degrade to an HTML comment without them, so
# these are safe to drop if your project never renders a code.
segno>=1.6
python-barcode>=0.15
